# Seafood Marketplace API

Rails API app for the Seafood Marketplace project.

## Docker run (recommended)

```bash
docker compose up --build api sidekiq
```

## Main endpoints

- `GET /health`
- `GET /api/v1/health`
- `GET /api/v1/marketplace`
