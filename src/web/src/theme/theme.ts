import { createTheme } from '@shopify/polaris';

export const theme = {
  colors: {
    surface: {
      // Customize surface colors if needed
    },
    // Add other color customizations here
  },
  // Add other theme customizations here
} as const;

export default theme;
